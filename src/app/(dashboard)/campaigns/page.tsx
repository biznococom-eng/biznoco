import { CampaignsList } from "@/components/campaigns/CampaignsList";

export const metadata = {
  title: "Chiến dịch · Biznoco",
};

export default function CampaignsPage() {
  return <CampaignsList />;
}
